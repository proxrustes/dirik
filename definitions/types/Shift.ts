export type Shift = {
    id: number,
    creatorId: number,
    locationId: number,
    location: string,
    startDate: string,
    availableCash: number,
    totalCash: number,
    totalMoney: number
}